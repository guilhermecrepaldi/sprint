package com.strava_matematica.design

import androidx.compose.ui.graphics.Color

object CanvasColors {
    val Background    = Color(0xFFF5EFE6)   // bege claro quente
    val Surface       = Color(0xFFEDE5D8)   // bege ligeiramente mais profundo
    val NodeFill      = Color(0xFFE8DDD0)   // nó padrão (pastel)
    val NodeBorder    = Color(0xFFB8A892)   // borda fina bege-tan
    val NodeActive    = Color(0xFFC4956A)   // cobre pastel — nó atual
    val NodeReview    = Color(0xFFD4A574)   // âmbar suave — revisão urgente
    val NodeLocked    = Color(0xFFDAD4CC)   // bloqueado (quase sem cor)
    val Edge          = Color(0xFFCEC3B4)   // linha muito fina, quase invisível
    val TextPrimary   = Color(0xFF3D2E1E)   // marrom escuro para contraste
    val TextSecondary = Color(0xFF8B7563)   // marrom médio
    val ModeNode      = Color(0xFFEAE0D0)   // modo: retângulo levíssimo
}
